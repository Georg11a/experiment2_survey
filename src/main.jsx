import React from 'react'
import ReactDOM from 'react-dom/client'
import Exp2Survey from './exp2_survey'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Exp2Survey />
  </React.StrictMode>,
)
